version: '3.8'

services:

  web:
    build:
      context: ..
      dockerfile: .devcontainer/django/Dockerfile
    volumes:
      - ..:/workspace:cached
    image: framelody:1.0  # 建構後的映像名稱
    container_name: framelody-container
    # ports:
    #   - "8000:8000"
    #   - "5555:5555"
    command: sleep infinity
    depends_on:
      - myredis


  myredis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
    # container_name: my_redis
    # command: sleep infinity
